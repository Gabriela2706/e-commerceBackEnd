export const postIntents = async (req, res) => {
  try {
    //se activa con el boton "pagar"
    //y desde aca llama a la APi elegida para el pago
    // no requiere service ya que toda la logica se terceariza
    //con la APi elegida
  } catch (error) {}
};
